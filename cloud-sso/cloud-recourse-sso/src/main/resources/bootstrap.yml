server:
  port: 8081
spring:
  application:
    name: cloud-recourse-sso
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
logging:
  level:
    cn.dszx: debug
